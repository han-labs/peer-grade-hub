# Database

Keep ERD files, schema decisions, and migration notes here.

The backend uses Flyway migrations in:

```text
backend/src/main/resources/db/migration
```

Use new migration versions after a schema has already been pushed and used by other members.
